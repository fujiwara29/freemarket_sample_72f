= render 'layouts/nav'
.pickup_inside_search
  .pickup_lists_search
    - if @category_items.nil?
      - @items.each do |item|
        = render partial: "items/item", locals: { item: item }
    -elsif @items.nil?
      - @category_items.each do |item|
        = render partial: "items/item", locals: { item: item }

= render 'layouts/banner'
= render 'layouts/footer'
= render 'layouts/sale-btn'